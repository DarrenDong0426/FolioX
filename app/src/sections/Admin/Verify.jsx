/**
 * Verify (placeholder)
 *
 * Will eventually read ?token=... from the URL, call /api/admin/verify,
 * and redirect to /Admin on success. For now, just renders a placeholder
 * so imports resolve.
 */
const Verify = () => {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">Verify</h1>
      <p className="text-sm text-neutral-600 mt-2">Verify page (placeholder)</p>
    </div>
  );
};

export default Verify;